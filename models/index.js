// 'use strict';

let fs = require('fs');
let path = require('path');
let config = require(__dirname + '/../../config/config.js');
let db = {"foo":"bar"};


//some other code for models

module.exports = db;